# Use the l10n package to add strings to the project. Do not hard-code strings, as they will have have to be translated.

# Always use the MVVM architecture and the repository-viewModel patterns to display data and edit data. Avoid using FutureBuilder and never make direct SQL database requests, but use the cache manager and the repositories.

# Add each step from your to-do list to the README.md file so you can keep up-to-date about the context in future sessions.

# Run the command "flutter analyze" frequently to check that that are no errors anywhere in the code and it can compile safely.